/// A Flutter widget that renders text with interpolated tappable links
/// using a simple template syntax.
///
/// Use `{{link text}}` for auto-indexed links or `{N{link text}}` for
/// explicitly indexed links, where N is a 1-based index into the
/// `urls` list.
library;

export 'src/linked_text.dart';
export 'src/linked_text_parser.dart';
